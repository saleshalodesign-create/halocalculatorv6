import { RatesConfig } from '../types';

export const DEFAULT_RATES: RatesConfig = {
  LIGHTBOX: 50,
  LIGHTBOX_W_BACKLIT: 85,
  BACKLIT: 35,
  TRANS: 45,
  PRINTED_3D: 120,
  VINYL_STICKER: 20,
  LED_STRIP: 17,
  ACRYLIC: 15,
};

export const LED_STRIP_DIVISOR_W = 39;
export const LED_STRIP_DIVISOR_H = 5;

export const CM_PER_INCH = 2.54;
export const MM_PER_INCH = 25.4;
export const INCHES_PER_FOOT = 12;

